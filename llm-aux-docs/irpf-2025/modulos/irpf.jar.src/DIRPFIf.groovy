
interface DIRPFIf {

	/**
	 * Retorna o mapa dos registros que serão alterados na transformação.
	 *
	 * @return Mapa dos registros.
	 */
	def getMapeamentoRegistros();

	/**
	 * Retorna o mapa dos campos associados ao mapeamento dos registros.
	 *
	 * @return Mapa dos campos.
	 */
	def getMapeamentoCampos();

}